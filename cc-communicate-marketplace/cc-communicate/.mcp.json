{
  "mcpServers": {
    "cc-communicate": {
      "command": "python",
      "args": ["-u", "${CLAUDE_PLUGIN_ROOT}/server/mcp_server.py"],
      "env": {
        "CLAUDE_PLUGIN_ROOT": "${CLAUDE_PLUGIN_ROOT}"
      }
    }
  }
}
