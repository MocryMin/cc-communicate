# Acceptance-Revision Review Brief (for kimi-k3)

> Prepared 2026-08-03 after the acceptance-revision phase was pushed
> (main `febc803` → `a8927a0`, tag **v0.4.0** on the delivery commit,
> origin/main). The customer's acceptance review
> (`docs/superpowers/reviews/2026-08-03-hardening-acceptance-review.md`)
> returned `REVISE_REQUESTED` with 6 AR proposals + 4 N-notes; the K3 task
> book (`docs/superpowers/plans/2026-08-03-acceptance-revision-plan.md`)
> was executed **inline** per its 执行约束 (edit `v2_win/` only →
> `tools/build_artifacts.py generate` → commit both trees).
> Gate state at brief time: **GATE PASS** — T0 syntax (44 .py + 2 .js),
> T1 pytest (220 tests), T2 parity (32 files) + T2 artifacts (33 files,
> templates pinned). Test record T50 in `tested&2betest.md` §1.

---

## 0. What the reviewer should know

- Project: cc-communicate — a Claude Code p2p plugin. Two byte-identical
  trees: `v2_win/` (Windows, canonical) and `v2_wsl/` (WSL2, GENERATED by
  `tools/build_artifacts.py generate`), parity-gated (HP-13-A, Wave 4).
- This phase revised the **deliverable**, not the architecture: Waves 1-4
  were `ACCEPTED_IN_PRINCIPLE`. All 6 AR + 3 N dispositions are DONE; the
  7 re-acceptance gates from the review §5 are mapped to evidence in §2.
- The delivery commit `a8927a0` contains: the code fixes + new tests, the
  corrected completion report (AR dispositions §9 + raw gate output), the
  acceptance review, the task book, and the T50 test record.
- Version/build identity: tag `v0.4.0` (annotated, on `a8927a0`) —
  replaces the pre-hardening v0.3.0; `cc-communicate-marketplace/` is
  marked **historical reference, not installable** (AR-05, option 2).

## 1. Dispositions (AR-01~06 + N-01~03)

| # | Requirement | Disposition | Evidence |
|---|---|---|---|
| AR-01 (P0) | Lock MCP major (fresh install broke on MCP 2.x: `mcp.server.fastmcp` gone) | `server/requirements.txt`: `mcp>=1.28` → `mcp>=1.28,<2` | 3 tests in `tests/unit/test_mcp_dependency_gate.py`: declaration pin; fresh-interpreter `from mcp.server.fastmcp import FastMCP` (env stripped of PYTHONPATH); dev-dep entry (N-03) |
| AR-02 (P0) | `listen_v2`/`query_my_cursors` must not mask transport failure as empty success | Per-side scan-success tracking (`rpc_client.call` raises `KernelError` locally; `call_remote` returns None remotely). Deadline decision: local zero success → `err(INTERNAL, retryable=True)`; local ok + host fail → result + `degraded_stores` marker; scanned messages never lost | 7 injection tests in `tests/unit/test_cursor_ack.py` (the 3 mandated failure classes + local-dead-with-remote-messages + full-success no-key + `query_my_cursors` pair) |
| AR-03 (P0) | `known_pids` deterministic bound (sorted-by-start_time raised TypeError on None/mixed at 9+ events) | `kernel.py _handle_start`: insertion-order trim `list(known.keys())[:-8]`; start_time only for PID-reuse validation. No schema change | 4 tests in `tests/unit/test_check_alive_fallback.py` (all-None, None+float mixed, PID dup + check_alive no regression, restart replay via `process_session_ctrl_event`) |
| AR-04 (P1) | resume/L2 must not be PASS + finding | L2 → `DEGRADED (T46)`: process/session restore succeeds, comm recovery fails on CC v2.1.220 (CC-side). Report §3.1 row + new §3.7 能力降级声明 (spawn-fresh fallback + CC-update re-test upgrade path); T45/T46 records; SKILL.md + README.md DEGRADED notes; run_regression L2 checklist notes DEGRADED recording | `tested&2betest.md` T45/T46; completion report §3.1/§3.7/§4.3; `v2_win/.../skills/cc-communicate/SKILL.md`, `README.md`; `tools/run_regression.py` L2 checklist |
| AR-05 (P1) | Closed, identifiable release surface | Option 2: `cc-communicate-marketplace/README.md` top banner (历史参考，不支持安装；权威实现 v2_win/ + v2_wsl/ build_artifacts.py 生成). Tag **v0.4.0** on the delivery commit. Delivery commit includes acceptance review + corrected completion report + task book + all fixes | `cc-communicate-marketplace/README.md`; tag v0.4.0; git log |
| AR-06 (P1) | HP-12/G4 contract: never write not-delivered as passed | HP-12 → `DEFERRED (分阶段接受)` consistently (§1.1/§2/§3.1/§3.4/§6.1/§8): H1 alternative observability (structured Result/Error incl. degraded markers + `backlog_stats` + `run_gc(dry_run)` + kernel log) + **restart condition** (进入 H2/H3 或第一次真实无法定位的传输故障). Master plan §4.1 phased wording (+§4.4 note) | completion report; `plans/2026-07-24-cc-communicate-hardening-master-plan.md` §4.1 |
| N-01 | `close_connection` degradation detail | Failed best-effort steps reported via `degraded_steps` in data (absent when clean → shape unchanged); still non-blocking, `closed` stays True | +1 test `test_close_connection_reports_degraded_steps` |
| N-02 | Regression output shows stderr | `run_regression.pytest_run` prints stderr tail on RED (missing-dep crashes emit no stdout) | +1 test `test_pytest_red_prints_stderr_tail` |
| N-03 | Dev-dependency entry | New `requirements-dev.txt` (pytest) at repo root; asserted by a gate test | `requirements-dev.txt`; `test_requirements_dev_declares_pytest` |

## 2. Re-acceptance gates (审核方 §5) — evidence map

| # | Gate | Evidence |
|---|---|---|
| 1 | Fresh environment per declared deps: MCP server imports/starts | `test_requirements_pins_mcp_major_1` + `test_mcp_fastmcp_imports_in_fresh_interpreter` (subprocess, env stripped); reviewer may re-run in an isolated venv with `pip install -r server/requirements.txt` |
| 2 | Original 204 + T0/T2 gates still green + new AR-01~03 regressions | 220 passed (204 + 16); raw output in §3 |
| 3 | `listen_v2`/`query_my_cursors` distinguish empty-success vs scan-failure | AR-02 injection tests (7); envelope `err(INTERNAL, retryable=True)` vs `ok` + `degraded_stores` |
| 4 | 9+ SessionStart replay with missing/mixed start_time: no crash | AR-03 tests (4), incl. real restart path `process_session_ctrl_event` |
| 5 | L2 DEGRADED + spawn-fresh fallback, no PASS claim | Report §3.7/§4.3 + T46/T45 + SKILL.md/README.md; grep "PASS + finding" in current-phase docs → only historical/corrective mentions remain |
| 6 | Artifact parity; single authoritative install entry; manifest tool count; version/build identity | T2 parity 32 + artifacts 33 green; `v2_win/.mcp.json` (20 FastMCP tools, unchanged since Wave 2); tag v0.4.0 == delivery commit; marketplace marked non-authoritative (AR-05) |
| 7 | Corrected completion report in the final commit with AR dispositions + raw output | `docs/superpowers/reviews/2026-08-03-hardening-completion-report.md` §9 (disposition table) + §4.1 (raw gate output) committed in `a8927a0` |

## 3. Raw gate output (brief time) + re-run commands

```
$ py -3 tools/run_regression.py
T0 syntax  PASS (44 .py + 2 .js parsed clean)
T1 pytest  PASS (220 passed in 18.43s)
T2 parity  PASS (PARITY OK (32 files compared, allowlist=['.mcp.json']))
T2 artifacts PASS (ARTIFACTS OK (33 files compared, templates pinned))
GATE: PASS
```

Re-run from a clean checkout: `pip install -r requirements-dev.txt`
(+ `-r v2_win/cc-communicate/server/requirements.txt`), then
`py -3 tools/run_regression.py`. Artifact identity:
`py -3 tools/build_artifacts.py verify`; 0-diff invariant:
`py -3 tools/build_artifacts.py generate` → `git diff --name-only v2_wsl` (empty).

## 4. Standing items (unchanged, recorded)

- **T46 re-test after a CC update** — the DEGRADED→PASS upgrade path for L2
  (AR-04); no cc-communicate code involvement.
- **N-04 stays deferred** — default data-root switch; count-based backlog
  (by-design boundary).
- **`cc-communicate-marketplace/`** — now an explicit historical reference
  (AR-05); no sync planned.
- Wave-4 tooling minors (dead constants, missing-template traceback, etc.)
  remain ship-as-recorded.
